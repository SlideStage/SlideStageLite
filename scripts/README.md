# Scripts

This folder is reserved for project automation.

Scripts:

- `build-fixtures.mjs`: generate deterministic `.hcslides` fixtures for tests.

Planned later scripts:

- `validate-fixtures.mjs`: check generated fixtures before browser tests.
